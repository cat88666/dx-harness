# Prompt-Ingest-Repo

用于 `/ingest-repo <repo>`：

1. 扫描仓库的 `README`、`pom.xml`、模块树、关键源码目录
2. 先识别仓库定位：框架层、业务层、基础设施层、样板层
3. 输出 1 个仓库页
4. 视情况输出 1-3 个模块页或任务页
5. 若存在明显主链路，再输出 1 个主题页
6. 更新 `index.md` 和 `log.md`

禁止：

- 一次性全量导入所有类
- 没有来源就写结论
- 把临时观察直接写成事故结论
