# Checklist-知识回写

适用：完成一次排障、需求分析、Review、链路梳理后。

1. 是否更新了已有页面，而不是重复新建
2. 是否补充了 `index.md`
3. 是否追加了 `log.md`
4. 是否写明了源码、日志、SQL 或配置来源
5. 若是排障结果，是否应该沉淀为 `runbooks/` 或 `incidents/`
6. 若是代码结构认知，是否应该沉淀为 `entities/` 或 `summaries/`
