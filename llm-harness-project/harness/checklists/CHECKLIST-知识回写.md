# Checklist-知识回写

适用：完成一次摄入、排障、需求分析、Review 或链路梳理后。

1. 是否优先更新了已有页面
2. 是否补充了 `index.md`
3. 是否追加了 `log.md`
4. 是否写明了源码、日志、SQL、配置、工单或事故来源
5. 若是排障结果，是否应该沉淀为 `runbooks/` 或 `incidents/`
6. 若是代码结构认知，是否应该沉淀为 `entities/` 或 `summaries/`
7. 是否避免把临时聊天过程写入 `wiki/`
